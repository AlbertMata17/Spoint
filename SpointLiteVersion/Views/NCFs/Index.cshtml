@model IEnumerable<SpointLiteVersion.Models.NCF>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NoNCF)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estatus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NoNCF)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.idNCF }) |
                @Html.ActionLink("Details", "Details", new { id=item.idNCF }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.idNCF })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
